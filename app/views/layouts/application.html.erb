<!DOCTYPE html>
<html>
    <head>
        <title>Spinkop<%= ' - ' + yield(:title) if content_for?(:title) %></title>
        <%= stylesheet_link_tag controller_name, media: 'all', 'data-turbolinks-track' => true %>
        <%= csrf_meta_tags %>
    </head>
    <body>

        <header>
            <nav>
                <%= link_to 'Accueil', controller: 'pages', action: 'home' %>
                <%= link_to 'Thèmes', :themes %>
                <%= link_to 'Questions', :questions %>
                <%= link_to 'Articles', :articles %>
            </nav>
        </header>

        <% if notice != nil %>
            <div class="notice wrapper">
                <p><%= notice %></p>
            </div>
        <% end %>

        <%= yield %>

        <%= javascript_include_tag controller_name, 'data-turbolinks-track' => true %>
    </body>
</html>
